# scalable-user-browser
A high-performance web application that efficiently loads and displays extremely large sorted datasets (up to 10 million items) using list virtualization techniques, with an alphabetical navigation menu for fast access without blocking the browser.
